declare type VoiceStreamEvents = {
    status: (status: 'listen' | 'started' | 'stopped') => void;
    hypotesis: (text: string, last: boolean) => void;
};
export declare type VoiceHandler = (data: Uint8Array, last: boolean) => void;
/**
 * Возвращает объект, позволяющий получать запись голоса пользователя и управлять ею.
 * @param createAudioProvider Источник голоса
 * @returns Api для запуска и остановки слушания
 */
export declare const createVoiceListener: (createAudioProvider?: (cb: (data: ArrayBuffer, last: boolean) => void) => Promise<() => void>) => {
    listen: (handleVoice: VoiceHandler) => Promise<void>;
    stop: () => void;
    on: <K extends "status" | "hypotesis">(event: K, cb: VoiceStreamEvents[K]) => () => void;
    readonly status: "listen" | "started" | "stopped";
};
export {};
//# sourceMappingURL=voiceListener.d.ts.map