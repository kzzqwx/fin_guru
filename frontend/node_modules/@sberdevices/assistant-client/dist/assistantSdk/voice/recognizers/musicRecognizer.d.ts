import { OriginalMessageType } from '../../../typings';
import { createVoiceListener } from '../listener/voiceListener';
export declare const createMusicRecognizer: (voiceListener: ReturnType<typeof createVoiceListener>) => {
    start: ({ sendVoice, messageId, onMessage, }: {
        sendVoice: (data: Uint8Array, last: boolean, messageName?: string | undefined) => void;
        messageId: number;
        onMessage: (cb: (message: OriginalMessageType) => void) => () => void;
    }) => Promise<void>;
    stop: () => void;
    readonly status: "inactive" | "active";
    readonly messageId: number;
};
//# sourceMappingURL=musicRecognizer.d.ts.map