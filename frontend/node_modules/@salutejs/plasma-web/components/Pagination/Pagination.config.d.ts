export declare const config: {
    defaults: {
        view: string;
        viewCurrentPage: string;
        size: string;
    };
    variations: {
        view: {
            secondary: string;
            clear: string;
            default: string;
            /**
             * @deprecated
             * использовать `default`
             */
            primary: string;
        };
        viewCurrentPage: {
            default: string;
            /**
             * @deprecated
             * использовать `default`
             */
            primary: string;
            secondary: string;
            clear: string;
        };
        size: {
            l: string;
            m: string;
            s: string;
            xs: string;
        };
        type: {
            compact: string;
            default: string;
        };
    };
};
