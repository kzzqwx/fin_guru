export declare const config: {
    defaults: {
        view: string;
        size: string;
    };
    variations: {
        size: {
            s: string;
            m: string;
        };
        view: {
            default: string;
        };
    };
};
