import { base as sizeCSS } from '../variations/_size/base';
import { base } from '../Typography.styles';
import { typographyRootCompose } from '../Typography';
export var textRoot = /*#__PURE__*/typographyRootCompose();
export var textConfig = {
  name: 'Text',
  tag: 'div',
  layout: textRoot,
  base: base,
  variations: {
    size: {
      css: sizeCSS
    }
  },
  defaults: {
    size: 'm'
  }
};