import { css } from 'styled-components';
import { StyledProgress, StyledTrack, StyledValue } from '../../Progress.styles';
import { tokens } from '../../Progress.tokens';
export var base = /*#__PURE__*/css(["", "{background:var(", ");}", "{background:var(", ");}", "{color:var(", ");}"], StyledTrack, tokens.trackBackgroundColor, StyledProgress, tokens.progressFilledBackgroundColor, StyledValue, tokens.valueColor);