'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
require('focus-visible');

/**
 * Компонент календаря с диапазоном.
 */
var boundCalendar = function boundCalendar(_ref) {
  var CalendarBase = _ref.base,
    CalendarDouble = _ref["double"],
    CalendarBaseRange = _ref.baseRange,
    CalendarDoubleRange = _ref.doubleRange;
  return function (rest) {
    if (!rest.isRange && !rest.isDouble) {
      return /*#__PURE__*/React.createElement(CalendarBase, rest);
    }
    if (!rest.isRange && rest.isDouble) {
      return /*#__PURE__*/React.createElement(CalendarDouble, rest);
    }
    if (rest.isRange && !rest.isDouble) {
      return /*#__PURE__*/React.createElement(CalendarBaseRange, rest);
    }
    if (rest.isRange && rest.isDouble) {
      return /*#__PURE__*/React.createElement(CalendarDoubleRange, rest);
    }
    return null;
  };
};

exports.boundCalendar = boundCalendar;
//# sourceMappingURL=Calendar.js.map
