'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var index = require('../utils/index.js');

/**
 * Хук для получения списка годов.
 */
var useYears = function useYears(date, startYear) {
  return React.useMemo(function () {
    // type-coverage:ignore-next-line
    var result = Array.from(Array(index.YEAR_RENDER_COUNT), function (_, i) {
      return {
        isCurrent: index.isCurrentYear(startYear + i),
        isSelected: index.isSelectedYear(date, startYear + i),
        yearValue: startYear + i
      };
    });
    return index.getMatrix(result, 3);
  }, [date, startYear]);
};

exports.useYears = useYears;
//# sourceMappingURL=useYears.js.map
