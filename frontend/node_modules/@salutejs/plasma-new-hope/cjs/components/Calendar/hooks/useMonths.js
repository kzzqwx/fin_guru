'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var index = require('../utils/index.js');

/**
 * Хук для получения списка месяцев.
 */
var useMonths = function useMonths(date) {
  return React.useMemo(function () {
    var result = index.SHORT_MONTH_NAME.map(function (monthName, monthIndex) {
      return {
        monthName: monthName,
        monthIndex: monthIndex,
        isCurrent: index.isCurrentMonth(date, monthIndex),
        isSelected: index.isSelectedMonth(date, monthIndex),
        monthFullName: index.MONTH_NAMES[monthIndex]
      };
    });
    return index.getMatrix(result, 3);
  }, [date]);
};

exports.useMonths = useMonths;
//# sourceMappingURL=useMonths.js.map
