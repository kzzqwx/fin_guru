/** Матрица радиусов. r - радиус стандартный, h - радиус округлый (вычисляемый из высоты)
 * tl ---- tr
 * |        |
 * |        |
 * bl ---- br
 */
export declare const pins: {
    'square-square': string;
    'square-clear': string;
    'clear-square': string;
    'clear-clear': string;
    'clear-circle': string;
    'circle-clear': string;
    'circle-circle': string;
};
export declare type Pin = keyof typeof pins;
export interface PinProps {
    /**
     * Границы компонента
     */
    pin: Pin;
}
/**
 * Преобразовывает матрицу радиусов с указанными размерами.
 * @param {string} matrixKey Ключ матрицы радиусов
 * @param {string} r r-радиус
 * @param {string} h h-радиус
 * @example
 * // Выведет значение для border-radius, равное '1em 0 0 1em'
 * convertPinsMatrix('r 0 0 r', '1em');
 * @example
 * // Выведет значение для border-radius, равное '1em 2em 2em 1em'
 * convertPinsMatrix('r h h r', '1em', '2em');
 */
export declare const convertRoundnessMatrix: (matrixKey?: "square-square" | "square-clear" | "clear-square" | "clear-clear" | "clear-circle" | "circle-clear" | "circle-circle" | undefined, r?: string | undefined, h?: string | undefined) => string | undefined;
