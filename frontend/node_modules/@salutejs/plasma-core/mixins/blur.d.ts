import { InterpolationFunction } from 'styled-components';
export declare const blurs: {
    small: string;
    medium: string;
    large: string;
};
export interface BlurProps {
    /**
     * Степень размытия фона
     */
    blur?: keyof typeof blurs;
}
export declare const applyBlur: InterpolationFunction<BlurProps>;
