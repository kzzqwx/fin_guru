import { css } from 'styled-components';
export var applyScrollSnap = function applyScrollSnap(_ref) {
  var scrollSnapAlign = _ref.scrollSnapAlign,
    scrollSnapStop = _ref.scrollSnapStop;
  return scrollSnapAlign && css(["scroll-snap-align:", ";scroll-snap-stop:", ";"], scrollSnapAlign, scrollSnapStop);
};