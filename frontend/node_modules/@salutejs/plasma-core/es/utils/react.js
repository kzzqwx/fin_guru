import React from 'react';
import ReactDOM from 'react-dom';

// NOTE: для next.js важно, чтобы путь до хука был полный, т.к. есть проблемы с ре-экспортом
import { useUniqId } from '../hooks/useUniqId';

// TODO: https://github.com/salute-developers/plasma/issues/233
// eslint-disable-next-line @typescript-eslint/no-explicit-any
var _ref = ReactDOM,
  flushSync = _ref.flushSync;
// eslint-disable-next-line @typescript-eslint/no-explicit-any
var _ref2 = React,
  useId = _ref2.useId;
export var IS_REACT_18 = /*#__PURE__*/ReactDOM.version.split('.')[0] === '18';
export function safeFlushSync(fn) {
  if (!IS_REACT_18) {
    return fn();
  }
  return flushSync(fn);
}
export var safeUseId = !IS_REACT_18 ? useUniqId : useId;