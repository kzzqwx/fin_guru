import React, { ReactNode, FunctionComponent } from 'react';
import { TabsProps } from './Tabs';
import { TabItemProps } from './TabItem';
export interface TabsControllerProps extends TabsProps {
    items: Array<{
        label: string;
        contentLeft?: ReactNode;
    }>;
    index: number;
    onIndexChange: (index: number) => void;
    children?: never;
    autoscroll?: boolean;
}
/**
 * Функция для создания `TabController`, которая дает возможность
 * кастомизировать стили, при этом сохраняя единый интерфейс и функционал.
 */
export declare function createTabsController<T extends HTMLDivElement, P extends TabsControllerProps>(ListComponent?: React.ForwardRefExoticComponent<TabsProps & React.RefAttributes<HTMLDivElement>>, ItemComponent?: FunctionComponent<TabItemProps>): React.ForwardRefExoticComponent<React.PropsWithoutRef<P> & React.RefAttributes<T>>;
