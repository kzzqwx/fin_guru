import styled from 'styled-components';
import { success, critical } from '@salutejs/plasma-core';
import { Caption } from '../Typography';
var statuses = {
  error: {
    color: critical
  },
  success: {
    color: success
  }
};

// NOTE: don't know how to fix ts error TS2742

export var UploadMessage = /*#__PURE__*/styled(Caption).withConfig({
  componentId: "plasma-web-core__sc-1jn4b7l-0"
})(["margin-top:0.25rem;text-align:center;", ""], function (_ref) {
  var status = _ref.status;
  return status && statuses[status];
});