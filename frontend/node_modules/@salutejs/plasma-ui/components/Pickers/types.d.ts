export declare type PickerSize = 'l' | 's' | 'xs';
export declare type PickerVisibleItems = 5 | 3;
export interface SizeProps {
    /**
     * Размер компонента
     */
    size?: PickerSize;
}
export declare type PickerItem = {
    value: string | number | Date;
    label: string | number;
    isVirtual?: boolean;
};
export declare const DEFAULT_PICKER_SIZE: PickerSize;
export declare const DEFAULT_VISIBLE_ITEMS: PickerVisibleItems;
export declare type DateType = readonly [number, number, number];
export declare type TimeType = readonly [number, number, number];
