/// <reference types="react" />
import { IconName as IName, IconSize } from '@salutejs/plasma-icons';
export interface IconSetProps {
    size?: IconSize;
    color?: string;
    exclude?: Array<IName>;
    include?: Array<IName>;
}
export declare const IconSet: ({ size, color, exclude, include }: IconSetProps) => JSX.Element;
