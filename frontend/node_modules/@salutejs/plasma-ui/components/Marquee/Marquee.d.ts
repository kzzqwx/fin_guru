import { ReactNode } from 'react';
interface MarqueeProps {
    children?: ReactNode;
    textAlign?: 'start' | 'center' | 'end';
}
/**
 * Компонент для отображения бегущей строки
 */
export declare const Marquee: ({ textAlign, children, ...rest }: MarqueeProps) => JSX.Element;
export {};
