import React from 'react';
import { ScrollAxis } from '@salutejs/plasma-core';
declare const throttlingParamsDefault: {
    leading: boolean;
    trailing: boolean;
};
/**
 * Хук для навигации. Слушает нажатие кнопок на пульте/клавиатуре.
 */
export declare function useRemoteHandlers({ initialIndex, axis, delay, longDelay, min, max, count, longCount, throttlingParams, }: {
    initialIndex: number;
    axis: ScrollAxis;
    delay: number;
    longDelay: number;
    min: number;
    max: number;
    count?: number;
    longCount?: number;
    throttlingParams?: typeof throttlingParamsDefault;
}): [number, React.Dispatch<React.SetStateAction<number>>];
export {};
