/// <reference types="react" />
import { CarouselItemProps } from '.';
/**
 * Функция сброса стилей элементов вне вьюпорта
 */
export declare const scaleResetCallback: (itemEl: HTMLDivElement) => void;
/**
 * Функция увеличения центрального элемента
 */
export declare const scaleCallback: (itemEl: HTMLDivElement, slot: number) => void;
export interface ScalingColCardProps extends Omit<CarouselItemProps, 'size' | 'sizeM'> {
    isActive: boolean;
    item: {
        title: string;
        imageSrc: string;
    };
}
export declare const ScalingColCard: ({ isActive, scrollSnapAlign, item, ...rest }: ScalingColCardProps) => JSX.Element;
