import React from 'react';
import { IconSize } from '@salutejs/plasma-icons';
import type { PickOptional } from '@salutejs/plasma-core';
import { ButtonProps } from '../Button';
export interface HeaderArrowProps extends PickOptional<ButtonProps, 'as' | 'size' | 'scaleOnInteraction' | 'disabled'>, React.ButtonHTMLAttributes<HTMLButtonElement> {
    /**
     * Тип стрелки.
     */
    arrow: 'back' | 'minimize';
    iconSize?: IconSize;
}
/**
 * Кнопка-стрелка с возможностью отображения в двух типах - "назад" или "свернуть".
 */
export declare const HeaderArrow: ({ arrow, iconSize, ...rest }: HeaderArrowProps) => JSX.Element;
