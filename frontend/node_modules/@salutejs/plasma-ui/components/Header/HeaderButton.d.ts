import React from 'react';
import type { PickOptional } from '@salutejs/plasma-core';
import { ButtonProps } from '../Button';
export interface HeaderButtonProps extends PickOptional<ButtonProps, 'as' | 'size' | 'scaleOnInteraction' | 'disabled'>, React.ButtonHTMLAttributes<HTMLButtonElement> {
}
/**
 * Кнопка без внутренних отступов, границ и цвета фона.
 */
export declare const HeaderButton: ({ size, children, ...rest }: HeaderButtonProps) => JSX.Element;
