/// <reference types="react" />
/**
 * Локальная копия иконки Done, полная сборка
 */
declare const sizeMap: {
    xs: number;
    s: number;
};
interface IconProps {
    size?: keyof typeof sizeMap;
    color?: string;
    className?: string;
}
export declare const IconDone: ({ size, color, className }: IconProps) => JSX.Element;
export {};
